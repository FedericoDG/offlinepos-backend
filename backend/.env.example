# Fallback local opcional — la config real vive en ../.env.example (backend_panel_administracion/.env)
# Ver backend/src/config/env.ts: carga primero ../../../.env y luego .env local como fallback.
# Si corrés el backend aislado (cd backend), copiá ../.env.example a ../.env o a .env.
DATABASE_URL="postgresql://usuarioappventas159753:passwordappventas159753@localhost:5432/ventasdb?connection_limit=10"
